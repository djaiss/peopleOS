meta {
  name: List contacts
  type: http
  seq: 4
}

get {
  url: http://{{URL}}/vaults/{{vault_id}}/contacts
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
