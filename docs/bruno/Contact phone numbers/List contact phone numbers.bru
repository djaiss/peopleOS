meta {
  name: List contact phone numbers
  type: http
  seq: 4
}

get {
  url: http://{{URL}}/vaults/{{vault_id}}/contacts/{{contact_id}}/phone-numbers
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
