meta {
  name: List post templates
  type: http
  seq: 1
}

get {
  url: http://{{URL}}/post-templates
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
